# Core dependencies
gradio>=4.0
groq>=0.4.0

# Text-to-Speech (Microsoft Edge TTS - free, no API key)
edge-tts>=6.1.0

# Audio processing
pydub>=0.25.1

# Note: pydub requires ffmpeg for MP3 conversion
# On HuggingFace Spaces, ffmpeg is pre-installed
# For local development: apt-get install ffmpeg (Linux) or brew install ffmpeg (Mac)
